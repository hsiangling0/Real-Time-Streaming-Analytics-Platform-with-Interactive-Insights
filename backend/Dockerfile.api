FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app_backend ./app_backend

ENV PYTHONPATH=/app

CMD ["sh", "-c", "uvicorn app_backend.main:app --host 0.0.0.0 --port ${PORT:-8080}"]